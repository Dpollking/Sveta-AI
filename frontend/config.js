// If the chat frontend is deployed separately from the backend (e.g. this
// file on Vercel, the API on Render), set this to the backend's public URL,
// no trailing slash: window.SVETA_API_BASE = "https://sveta-ai-api.onrender.com";
// Leave empty when frontend and backend are served from the same origin.
window.SVETA_API_BASE = "";
